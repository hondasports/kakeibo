# 単独Astraと任意の委譲

通常は単独Astra。サブエージェントはユーザーの明示依頼時だけ使う。model/effortはランタイム側の設定であり、この文書の記載では変更されない。

委譲する場合の入力は問い、契約、対象差分、証拠、編集可能範囲。read-onlyレビューを基本とし、同じ差分を複数writerに書かせない。返却は根拠と指摘。自己レビューと独立レビューを区別し、必要な人間承認は置き換えない。
